{
  "buildCommand": "git clone https://github.com/flutter/flutter.git --depth 1 -b 3.22.3 flutter-sdk && export PATH=\"$PATH:`pwd`/flutter-sdk/bin\" && flutter config --enable-web && flutter doctor && flutter pub get && flutter build web --release",
  "outputDirectory": "build/web"
}